version: 2

models:
  - name: dim_vendors
    description: "This table contains vendor information."
    columns:
      - name: vendor_id
        description: "The unique identifier for each vendor."
  
      - name: payment_type
        data_tests:
          - accepted_values:
              arguments:
                values: [1, 2, 3, 4, 5]
                quote: false